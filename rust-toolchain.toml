# One Rust toolchain for everyone and for CI, so a new stable release
# can't add Clippy lints that fail the build out of the blue. Bump
# deliberately (and fix any new lints in the same PR).
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
